I want to make a desktop application in Suket-AutoReplayUploader that builds on the work in AutoReplayUploader.

AutoReplayUploader is a plugin that is part of a now obsolete injection tool, Bakkesmod, that injects itself into Rocket League.
I want to be able to continue this auto replay uploading to an extent:

- Whenever a replay gets saved into the directory where replays are saved, that replay will get uploaded to Ballchasing in the same way the existing implementation uploads it.
- This application will check for new ".replay" files in the given folder every `x` minutes (constant that can be changed), and if it exists, it will list itself in the UI, and there'll be an option to auto upload it or press a button to upload it.
    - If it auto uploads, AND it is successful, it will delete the file locally. If it is not successful due to some failures, it will try uploading again in the next cycle (after `x` minutes). If it fails due to the daily/weekly limits, it will return that and show it in the UI. More details about what to do in that scenario will be in the future.
    - If the user does not turn on the auto uploading feature, they will have to press a button to upload the files from the last uploaded point. If it is unsuccessful, it will show that in the UI and ask the user to try again. If it fails due to daily/weekly limits, it will return that and show it in the UI.
- After a successful upload, the ".replay" file will be deleted.
- There will be an option to uncheck replays to be uploaded.

You MUST tell me if any of these requirements are not possible due to it requiring any relation to the Bakkesmod injection mod. Ask me for any details regarding the implementation.

-----

I have run the app, and here are ideas I had:

- Make it easier to select/deselect a replay: I don't want to have to click on a small check mark to select/deselect a replay.
- Add an option to hide selected replays from the list so the list isn't cluttered with unwanted replays.
    - Also add an option to manually add a replay file to the list by opening up file explorer and selecting the replay file. If this replay file isn't in the folder, add it to the folder.
    - This behaviour should reflect once the app is closed and reopened, so if I hide a replay, close the app, and reopen it, it should not display again until I manually add the replay file.
- When uploading a replay, the server will respond with 3 codes (can't respond 400 as we won't have a Bad Request):
    - 201 Created: upload successful
    - 409 Conflict: replay already exists. In this case, show that the upload failed but also link the user to the replay online, which is in the "location" field of the response body of the 409.
    - 500 Server Error: Since this could be possible, implement a retry policy that repeats 3 times with exponential backoff (with initial backoff 3s), and if it still fails, show the user that the upload failed and to try again in a bit. Make it obvious that the retry is happening by making a new status which shows that it is retrying.
- If the user hasn't hidden a successfully uploaded replay, auto hide it from the list after 3 hours.
- If possible, obtain the map (from making a GET request to https://ballchasing.com/api/maps, which gets a list of map codes to map names (map as in stadium), score line and players in each team to allow the user to see more clearly what replay they are trying to upload.
- Fix the spacing in the settings menu

Implement the suggestions above, and ask me any questions about this if needed.

---

Here are some more suggestions:

- The "Add replay file...", "Hide Selected" and "Refresh" buttons are hard to see. Replace the black text with white.
- Amend the behaviour of hiding as follows:
    - Hidden replays should not be visible at all in the app. If a replay gets hidden, the only way the user should be able to add it back would be via the add replay file button. Remove the "Show Hidden" button and "Clear Hidden List" buttons. 
- Make the replay more readable:
    - Extract information from the replay such as the map, score line, and players on each team and show them in the list view. Look at AutoReplayUploader to see if this is possilbe, otherwise let me know that this isn't possible, and remove any functionality added regarding maps.
- Move the upload button / auto uploading section to the bottom of the app, separating them from the other buttons.
- For the Auto Uploading state, display the wait timer. Also keep the upload button visible, and if the user presses it, it resets the timer.

Implement the suggestions.

--- v HAVEN'T DONE THIS ONE YET v

Here are some more suggestions:

- Include a "Ballchasing Status" indicator at the top:
    - It does `GET https://ballchasing.com/api`:
        - on 200, show green as connection successful
	- on 401, show red with it saying api key is invalid
        - on 500, show yellow with server error, and retry three times (like before)
        - Ping this endpoint before doing any uploading to catch errors/issues before mass-uploading.
    - When the user updates the API key, this status gets updated - otherwise this will get pinged every 4mins.